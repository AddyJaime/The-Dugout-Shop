{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  // se escluye porque no lo necesitamos al momento de compilar
  // nuestro codigo ni se necesita en prod solo para ocrrer algunos coMANDOS DEL CLI
  "exclude": ["prisma.config.ts"]
}
